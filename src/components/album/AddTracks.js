function addTrack(trackId) {
  // Fonction pour ajouter une chanson a une liste
  return trackId;
}


function addAlbum(albumId) {
  // Fonction pour ajouter l'album a une liste
  // [tracksId] = getAlbumTracks(albumId,)
  // addTrack(trackId)
  return albumId;
}


export { addTrack, addAlbum };
